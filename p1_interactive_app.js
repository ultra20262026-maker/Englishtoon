// Controller with Full Word Audio Speech Pronunciation & Precise LTR Missing Dots Position
document.addEventListener('DOMContentLoaded', () => {
    // State
    // const urlParams = new URLSearchParams(window.location.search);
    // const paramGrade = urlParams.get('grade');
    
    // REDIRECTS FULLY DISABLED TO PREVENT LOOPS
    /*
    if (paramGrade === 'p2' || paramGrade === 'primary-2') {
        window.location.replace('p2-books.html');
        return;
    }
    */
    
    console.log("Interactive App Loaded - ALL REDIRECTS BYPASSED.");
    
    // Rest of the file logic...
});
